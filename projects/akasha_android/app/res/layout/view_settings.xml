<?xml version="1.0" encoding="utf-8"?>
<!-- 全局设置主页（索引）。行由 SettingsPanel 动态生成: 标题 + 当前值摘要 + ›。
     外层 activity_settings 已有 ScrollView 保证整体可上下滑动。 -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="12dp">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="── 全局设置 ──"
        android:textStyle="bold"
        android:textColor="#0a66c2"/>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:text="点击任一条目进入子页面设置；下方小字为当前值摘要。"
        android:textSize="11sp"
        android:textColor="#999999"/>

    <!-- 动态索引行容器（SettingsPanel.onResume 填充） -->
    <LinearLayout
        android:id="@+id/llSettingsIndex"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_marginTop="8dp"/>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:textSize="11sp"
        android:textColor="#999999"
        android:text="提示: 单个 Agent 的 Base URL / API Key / 自启开关 / 权限 / 模型级 Prompt，请前往「通讯录」→ 对应 Agent 中设置；会话级 Prompt 在「对话→设置」中设置。"/>

    <!-- 底部留白 -->
    <View
        android:layout_width="match_parent"
        android:layout_height="20dp"/>
</LinearLayout>
